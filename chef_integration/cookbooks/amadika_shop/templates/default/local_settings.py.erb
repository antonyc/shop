import os
MEDIA_ROOT = "<%= @media_root  %>"
ROOT_PATH = "<%= @www_path  %>/src/university"

DEBUG = True

HOST_NAME = "uni:8080"

TEMPLATE_DIRS = (
    os.path.join(ROOT_PATH, 'templates'),
)

STATIC_ROOT = os.path.abspath(os.path.join(ROOT_PATH, '..', '..' ,'static', 'blocks'))

STATICFILES_DIRS = [
    os.path.abspath(os.path.join(ROOT_PATH, '..', '..' ,'static')),
]

LOCALE_PATHS = (os.path.abspath(os.path.join(ROOT_PATH, '..', '..' ,'locale')), )

MONGO_DATABASES={
    'default' : {
        'NAME': 'timetable',
        'HOST': '127.0.0.1',
        },
    }

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': '<%= @mysql_name  %>',                      # Or path to database file if using sqlite3.
        'USER': '<%= @mysql_username  %>',
        'PASSWORD': '<%= @mysql_password  %>',
        'HOST': '',                      # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
        'TEST_CHARSET': 'utf8',
        'TEST_COLLATION': 'utf8_general_ci',
        'OPTIONS': {
            'init_command': 'SET storage_engine=INNODB; SET names utf8;'
        },
    }
}

COMPRESS = False

